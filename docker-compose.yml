version: '3.8'

services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development

  # backend:
  #   build: ./backend
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./backend:/app
  #     - /app/node_modules
  #   depends_on:
  #     - db
  #   environment:
  #     - NODE_ENV=development
  #     - DB_HOST=db
  #     - DB_PORT=3306
  #     - DB_USER=root
  #     - DB_PASSWORD=password
  #     - DB_NAME=kalkulator

  # db:
  #   image: mysql:8.0
  #   ports:
  #     - "3306:3306"
  #   environment:
  #     MYSQL_ROOT_PASSWORD: password
  #     MYSQL_DATABASE: kalkulator
  #   volumes:
  #     - mysql_data:/var/lib/mysql

# volumes:
#   mysql_data: